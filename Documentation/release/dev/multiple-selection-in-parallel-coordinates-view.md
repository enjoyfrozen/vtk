## Multiple Selection in the parallel coordinates view

Add support for multiple selection in the same axis in parallel coordinates view.
We can also now perform an addition, subtraction or toggle selection.
